const MIN_STAKE_AMOUNT = 1000000; // 1 STX in micro-STX
const MAX_STAKE_AMOUNT = 1000000000000; // 1,000,000 STX
const DEFAULT_LOCK_PERIOD = 144; // ~1 day in blocks
const REWARD_RATE_DENOMINATOR = 10000;
const CONTRACT_NAME = 'staking';

module.exports = {
  MIN_STAKE_AMOUNT,
  MAX_STAKE_AMOUNT,
  DEFAULT_LOCK_PERIOD,
  REWARD_RATE_DENOMINATOR,
  CONTRACT_NAME,
};
